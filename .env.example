# Appwrite Configuration
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your-project-id-here

# Todo Database Configuration
VITE_APPWRITE_DATABASE_ID=todo_db
VITE_APPWRITE_TODO_COLLECTION_ID=todos
VITE_APPWRITE_WORKSPACES_COLLECTION_ID=workspaces

# Instructions:
# 1. Replace 'your-project-id-here' with your actual Appwrite project ID
# 2. Create a database in Appwrite console with ID 'todo_db' (or change the ID above)
# 3. Create collections in the database:
#
#    a) Collection 'todos' with attributes:
#       - title (string, required)
#       - description (string, optional)
#       - completed (boolean, required, default: false)
#       - priority (string, required, default: "medium")
#       - dueDate (string, optional)
#       - userId (string, required)
#       - workspaceId (string, required)
#
#    b) Collection 'workspaces' with attributes:
#       - name (string, required)
#       - description (string, optional)
#       - color (string, required, default: "#3B82F6")
#       - userId (string, required)
#       - isDefault (boolean, optional, default: false)
#
# 4. Set appropriate permissions for both collections:
#    - Read: users
#    - Write: users
#    - Update: users
#    - Delete: users